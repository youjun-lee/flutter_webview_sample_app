import 'package:flutter/material.dart';

class WebViewFlutterScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Empty Web View Screen'),
      ),
      body: Center(
        child: Text('This is an empty page'),
      ),
    );
  }
}
